{
    "name": "syncpad-backend",
    "version": "1.0.0",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "bun --watch run src/index.ts",
        "lint": "tsc --noEmit && biome check --write"
    },
    "dependencies": {
        "@syncpad/shared": "*",
        "dotenv": "^17.2.3",
        "zod": "^4.1.13"
    },
    "devDependencies": {}
}
